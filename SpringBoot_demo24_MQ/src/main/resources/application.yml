server:
  port: 80
spring:
  activemq:
    broker-url: tcp://localhost:61616
  jms:
    template:
      default-destination: destination_application_id
    pub-sub-domain: true
  rabbitmq:
    host: localhost
    prot: 5672